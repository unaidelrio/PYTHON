# nombres = ["Jon", "Maria","Juan","Javier"]
# nueva_lista= [nombre for nombre in nombres if nombre[0] == "J"]


# for nombre in nombres:
#     if nombre[0]=="J":
#         nueva_lista.append(nombre)


#Lista para sacar los numeros positivos
#crear lista lista positiva lp que contenga los numeros positivos en la lista "numeros"
# numeros = [3, 54, -13, 4, -98, 99, -123]
# lp = [np for np in numeros if np < 0] #lp = lista positiva, np = numero positivo
# print(lp)

# ln = [nn/2 for nn in numeros if nn >= 0] #ln = lista negativa, nn = numero negativo
# print(ln)

# lp = [np for np in numeros if not np <= 0] #lp = lista positiva, np = numero positivo
# print(lp)

